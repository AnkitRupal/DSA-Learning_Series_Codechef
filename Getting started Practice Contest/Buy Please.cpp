#include<bits/stdc++.h>
using namespace std;

/*
	BUY PLEASE
	Total Price = (Number of Pens * Cost of one Pen) + (Number of Pencils * Cost of one Pencil)
*/

int main() {
	int a, b, x, y;
	cin >> a >> b >> x >> y;
	cout << (a * x) + (b * y) << "\n";
	return 0;
}